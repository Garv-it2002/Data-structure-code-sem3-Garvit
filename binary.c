#include<stdio.h>
int main()
{
    int i,low,high,mid,n,key,array[100];
    printf("Enter the number of elements\n");
    scanf("%d",&n);
    printf("Enter %d intigers\n",n);
    for(i=0;i<n;i++)
        scanf("%d",&array[i]);
    printf("Enter key\n");
    scanf("%d",&key);
    low=0;
    high=n-1;
    mid=(low+high)/2;
    while(low<=high)
    {
        
    }
}